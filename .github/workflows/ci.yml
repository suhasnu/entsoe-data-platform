name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install
        run: pip install -e ".[dev]"

      - name: Lint
        run: ruff check .

      - name: Format check
        run: ruff format --check .

      - name: Type check
        run: mypy src

      - name: Test
        # Integration tests need BigQuery credentials, so CI covers unit tests only.
        env:
          ENTSOE_API_KEY: ci-placeholder
          GCP_PROJECT_ID: ci-placeholder
        run: pytest

  dbt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dbt
        run: pip install "dbt-core>=1.8,<2" "dbt-bigquery>=1.8,<2"

      - name: Check models compile
        working-directory: dbt
        env:
          GCP_PROJECT_ID: ci-placeholder
          BQ_LOCATION: EU
          GOOGLE_APPLICATION_CREDENTIALS: /dev/null
        run: dbt parse --profiles-dir .